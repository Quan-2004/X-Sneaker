# Netlify Configuration for X-Sneaker

[build]
  # Publish directory (root of the site)
  publish = "."
  
  # Command to run before building (if needed)
  # command = "echo 'No build command needed'"

# Redirect rules
[[redirects]]
  # Redirect old /html/* URLs to new root URLs for SEO
  from = "/html/*"
  to = "/:splat"
  status = 301
  force = true

# Headers for security and caching
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

# Cache CSS and JS files for 1 year
[[headers]]
  for = "/css/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
    
[[headers]]
  for = "/js/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Cache images for 1 month
[[headers]]
  for = "/image/*"
  [headers.values]
    Cache-Control = "public, max-age=2592000"

# Ensure correct MIME types
[[headers]]
  for = "*.css"
  [headers.values]
    Content-Type = "text/css; charset=utf-8"

[[headers]]
  for = "*.js"
  [headers.values]
    Content-Type = "application/javascript; charset=utf-8"

# SPA-like behavior - serve index.html for unknown routes (optional)
# [[redirects]]
#   from = "/*"
#   to = "/index.html"
#   status = 200
